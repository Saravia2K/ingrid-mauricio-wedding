// Declare missing module for moment locale side-effect import
declare module "moment/locale/es";

// If you later import other moment locales and see the same error,
// add similar `declare module` lines for them here.
